# Server Configuration
SERVER_ADDRESS=:8080
BASE_URL=http://localhost:8080

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=meetwhen
DB_PASSWORD=meetwhen
DB_NAME=meetwhen
DB_SSLMODE=disable
MIGRATIONS_PATH=migrations

# Application Configuration
APP_ENV=development
# Generate with: openssl rand -hex 16 (must be 32 characters)
ENCRYPTION_KEY=

# Google OAuth (for Google Calendar and Google Meet)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URL=http://localhost:8080/auth/google/callback

# Zoom OAuth
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=
ZOOM_REDIRECT_URL=http://localhost:8080/auth/zoom/callback

# Email Configuration
EMAIL_PROVIDER=smtp
EMAIL_FROM_ADDRESS=noreply@localhost
EMAIL_FROM_NAME=Meet When

# SMTP Configuration
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=

# Mailgun Configuration (alternative to SMTP)
MAILGUN_DOMAIN=
MAILGUN_API_KEY=

# Production settings
# DOMAIN=meet.yourdomain.com
# TLS_EMAIL=admin@yourdomain.com
